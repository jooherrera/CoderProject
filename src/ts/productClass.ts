class Product {
  price: number;
  image: string;
  title: string;

  constructor(title : string,image : string,price : number) {
    this.title = title;
    this.image = image;
    this.price = price;
  }
}